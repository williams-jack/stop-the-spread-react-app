// Add a loction
const addLocation = () => {
    // Use axios to make call to back end
};
// Edit a location
const editLocation = () => {
    // Use axios to make call to back end
};
// Delete a location
const deleteLocation = () => {
    // Use axios to make call to back end
};
// Get locations
const getLocations = () => {
    // Use axios to make call to back end
};
